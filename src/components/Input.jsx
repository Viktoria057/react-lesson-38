const Input = ({ placeholder, type, onChange }) => {
    return (
      <input
        placeholder={placeholder}
        type={type}
        onChange={onChange}
      />
    );
  };
  
  export default Input;
  